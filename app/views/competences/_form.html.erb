<%= simple_form_for @competence do |f| %>

  <div class="form-inputs">
    <%= f.input :titre, placeholder: "Titre" %>
    <%= f.input :texte, placeholder: "Description" %>
  </div>

  <div class="form-actions w-100 d-flex justify-content-end">
    <%= f.submit "Enregistrer ma compétence", class: "btn btn-ghost" %>
  </div>

<% end %>
